{
  "version": 1,
  "integrations": [
    {
      "key": "stripe_checkout",
      "name": "Stripe Checkout",
      "secrets": [
        {
          "key": "STRIPE_SECRET_KEY",
          "label": "Stripe secret key",
          "helpText": "Server-only key used to create hosted Checkout Sessions.",
          "required": true,
          "environments": ["local", "preview", "production"]
        },
        {
          "key": "STRIPE_WEBHOOK_SECRET",
          "label": "Stripe webhook signing secret",
          "helpText": "Signing secret used to verify Stripe webhook events.",
          "required": true,
          "environments": ["local", "preview", "production"]
        }
      ]
    }
  ]
}
